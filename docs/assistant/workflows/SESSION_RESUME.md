# Session Resume

## Current State

No active roadmap is open for this worktree. Use normal ExecPlan flow unless the user explicitly asks for roadmap or master-plan governance.

## Resume Order

1. Open `APP_KNOWLEDGE.md` for canonical product context.
2. Open `agent.md` for runbook rules.
3. If the task is harness-related, use `docs/assistant/workflows/PROJECT_HARNESS_SYNC_WORKFLOW.md`.
4. If an ExecPlan is active, resume from `docs/assistant/exec_plans/active/`.
